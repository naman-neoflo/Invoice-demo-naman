# ============================================================
#  ROOT .env — used by docker-compose for variable substitution
#  Copy this to .env and fill in your values:
#    cp .env.example .env
# ============================================================

# ── Anthropic / Claude AI ────────────────────────────────────────────────────
# Used by: frontend Neo chat (/api/neo/chat) + backend Cash AI matching
ANTHROPIC_API_KEY=sk-ant-...

# ── Frontend auth credentials ────────────────────────────────────────────────
# Login page username / password and session cookie value.
# Defaults are already set in the code — only change if you want custom creds.
AUTH_USERNAME=neoflo
AUTH_PASSWORD=n30fl0r0x0r!
AUTH_SESSION_TOKEN=ok-2026
